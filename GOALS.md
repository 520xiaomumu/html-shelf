# 页架（html-shelf）产品目标

本文档是产品合同。实现、分期、演示验收都以本文为准。以后要改范围，先改本文再改代码。

## 一句话

把本地 HTML 文件当应用来分类、启动；顺带一个够用的 Chromium 浏览器（打开网页、播视频、语音通话），并走向兼容 Chrome 插件商店。

## 产品原则（不可破）

1. 主界面是 HTML 应用库，不是浏览器的新标签页附件。
2. 打开一个 html 要像打开 App：一次点击，独立窗或专注视图，地址栏默认可收起。
3. 分类要轻：分类（可嵌套一层）+ 标签 + 搜索，支持拖入文件/文件夹。
4. 浏览器宁少勿假：v1 只要打开网页、HTML5 音视频、WebRTC 通话；不做账号同步、不做自研广告拦截。
5. 插件：最终目标是兼容谷歌浏览器插件市场；v1 必须能加载本机 unpacked 目录和 .crx，商店安装放 v2。
6. UI 默认中文。
7. 任何本地服务只绑 127.0.0.1，禁止 0.0.0.0。
8. 每个本地 HTML 应用默认沙箱，禁止 nodeIntegration，不把 Node 能力暴露给页面。

## 谁用、解决什么

用户手里有很多单文件或小目录 HTML（工具、游戏、实验页），不想在文件管理器里找路径，也不想把它们埋进 Chrome 书签。页架是这些 HTML 的桌面/手机主屏。

典型场景：

- 把一堆本地工具页（计时、换算、便签、生成器）扫进库，按「工具 / 游戏 / 实验」分类，搜索后一键打开。
- 打开后像独立应用：专注视图，地址栏默认可收起，localStorage 不和其他应用串。
- 偶尔当普通浏览器用：多标签打开网页、播视频、页面里的 WebRTC 通话能跑。
- 开发者本地调试 unpacked 扩展，或导入本机 .crx，在普通网页标签里验证是否已加载。

不解决：替代完整 Chrome、同步账号、当广告拦截器、把别人的网站打包上架。

## 平台与分期（写死，避免以后扯皮）

### 桌面 v1（当前必须交付）

Linux（含云电脑演示）、Windows、macOS 能构建；云电脑 Ubuntu + X11 必须能跑起来做验收。

技术：Electron（嵌入 Chromium）。不选 Tauri / 系统 WebView 做桌面主壳，否则视频、WebRTC、扩展会假。

必须有：

- 库：添加文件、添加文件夹（扫描 .html/.htm）、分类、标签、搜索、排序（最近打开/名称）、卡片封面（可自定义图；没有则用首字母或色块）、重命名显示名、从库移除（不默认删磁盘文件）、打开方式（应用窗 / 普通浏览器标签）。
- 启动：单击卡片 1 秒内打开；每个应用独立 persist 分区（localStorage/cookie 不串）；相对路径资源（css/js/图）要能加载；刷新不丢库。
- 浏览器壳：多标签、地址栏、后退/前进/刷新/主页（回库）、缩放、新标签打开 URL、窗口全屏。
- 媒体：HTML5 audio/video、全屏播放；WebRTC getUserMedia 权限提示可走通（麦克风/摄像头）。语音通话不自研信令，页面里的 WebRTC 能跑即可。
- 插件：设置页列出已加载扩展；从本机目录加载 unpacked；导入 .crx 若 Electron 版本允许；开关；卸载。v1 不要求 Chrome 网上应用店一键安装。
- 数据：库索引存在用户数据目录（electron userData），JSON 即可，不上传、不要求登录。
- 权限：用户把路径加入库才可读该文件/其目录；禁止页面随便读盘。
- 中文 UI，木质或干净的桌面感都可以，但不要游戏像素风。
- README 写清如何在 Linux 安装依赖并 `npm start`。
- 带最少自动化测试：库的分类/扫描/隔离逻辑用 Node 测试跑绿。

明确不做（v1）：

- 自研渲染引擎或 fork 整份 Chromium 源码
- Chrome 账号同步、密码管理器、自研广告拦截
- iOS Chrome 插件（系统不允许，永远不要承诺）
- 把第三方网站打包去应用商店
- 0.0.0.0 绑定、远程调试端口对外、号池

### 桌面 v2

- 尽量对接 Chrome Web Store 安装（若 Electron 做不到，评估嵌入完整 Chromium / 说明缺口，不许假装已支持）
- 多配置文件、拖拽分类、库导出/导入

### 安卓（v1 之后）

同一套库数据模型；浏览 + 启动 HTML；插件另开方案（不能假装和桌面商店一样）。

### iOS（更后）

库 + 系统 WebView 浏览；永久无 Chrome 插件。在文档里写死。

## 信息架构

- 主屏：左侧分类，右侧卡片网格，顶栏搜索和「添加」
- 应用窗：默认隐藏完整浏览器 chrome，右上角可展开地址栏
- 浏览窗：标准标签浏览器
- 设置：扩展、权限、库文件位置

补充约定：

- 启动应用默认走「应用窗」。用户可在卡片上改成「普通浏览器标签」。
- 「主页」始终回到库，不回到上次浏览的网站。
- 分类树只允许一层子分类：根分类 → 子分类，子分类不能再嵌套。
- 拖入文件/文件夹是添加库条目的一等入口，与菜单「添加文件 / 添加文件夹」等价。

## 数据模型

```
Item: id, title, path, kind (file|folder|url), categoryId, tags[], coverPath, lastOpened, openMode (app|tab), createdAt
Category: id, name, order, parentId (nullable, 只允许一层子分类)
Extension: id, name, sourcePath, enabled
LibraryState JSON in userData.
```

约束：

- Item.path 是用户明确加入库的本地路径或用户输入的 URL；kind=folder 表示扫描来源，扫描出的每个 `.html/.htm` 仍落成独立 `kind=file` 条目。
- 从库移除只删索引，不默认删磁盘文件。
- 每个 Item 打开时使用独立 persist 分区，key 与 id 绑定，禁止共用默认 session。
- Category.parentId 非空时，其父分类的 parentId 必须为空。
- Extension.sourcePath 仅指向本机 unpacked 目录或已导入的 crx 来源；v1 不存商店商品 ID。
- 库 JSON 只写 electron userData，不上传、不要求登录、不进 git。

## 成功标准（v1 演示必须当场能验）

1. 扫入一个含多个 html 的文件夹，分成至少两类，搜索可用。
2. 点卡片进入页面，相对资源正常，关掉再开库还在。
3. 地址栏打开一个能播的视频页或本地 mp4 能播。
4. 权限下 getUserMedia 不崩（无摄像头也要有明确失败提示，不能白屏）。
5. 加载一个 unpacked 扩展后，在普通网页标签里能看出已加载（哪怕只是扩展的 popup/enable 状态正确）。
6. 云电脑 Linux `npm start` 能出窗口。

## 验收时的演示素材

仓库内提供 examples/ 下 3 个小 HTML（工具、游戏占位、实验页），方便分类演示。不要抄受版权保护的游戏。

建议演示路径：

1. 添加文件夹扫入 examples/，分成「工具 / 游戏 / 实验」两类以上。
2. 搜索其中一个标题，点卡片 1 秒内打开应用窗。
3. 关掉应用再开，库还在；再开另一个条目，确认存储不串。
4. 浏览窗地址栏打开可播视频页或本地 mp4。
5. 打开实验页申请摄像头/麦克风：有权限提示；无设备时明确失败，不白屏。
6. 设置里加载一个 unpacked 扩展，到普通网页标签核对已加载状态。

## 安全与绑定（再写一遍，避免先打通再收）

- 任何本地 HTTP/WS 服务只绑 `127.0.0.1`。禁止 `0.0.0.0`，禁止把远程调试端口暴露到非本机。
- 本地 HTML 页面：`nodeIntegration: false`，不预加载 Node API，不把文件系统、shell、任意路径读能力交给页面。
- 页面只能访问用户加入库时批准的那个文件及其目录内的相对资源。
- 不实现号池、不实现对外远程调试、不把 userData 同步到第三方。

## 文档与测试义务

- README 必须用中文写清：这是什么、v1 范围、Linux 依赖、`npm start`、指向本文为合同。Chrome 网上应用店安装属于 v2。
- 最少自动化测试覆盖：文件夹扫描 `.html/.htm`、分类一层嵌套约束、条目 persist 隔离（不同 id 不共享存储键）。Node 测试必须跑绿。
